/**
 * Object that stores encoded images from SkyWeather.
 */
export interface MediaStorage {
    tempHumidity: string;
    skyCamera: string;
    barometerLightning: string;
    powerVoltage: string;
    powerCurrent: string;
}